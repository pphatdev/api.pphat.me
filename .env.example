# Github
GITHUB_CLIENT_SECRET=...
GITHUB_CLIENT_ID=...

# Google
GOOGLE_CLIENT_SECRET=...
GOOGLE_CLIENT_ID=...

JWT_SECRET=a-long-random-string
APP_URL=http://localhost:8787


SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=info.sophat@gmail.com
SMTP_FROM="Sophat.L <info.sophat@gmail.com>"
SMTP_PASS=your-email-password


CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_D1_DATABASE_ID=
CLOUDFLARE_API_TOKEN=

# Rate limits are configured on the Cloudflare Rate Limiting bindings in
# wrangler.jsonc — not via env vars. Edit the `limit` field of each binding
# in the `ratelimits` array there. Period is fixed at 60 seconds per binding.