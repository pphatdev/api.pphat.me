{
  "name": "api-pphat-me",
  "version": "0.8.4",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "migrate": "npx wrangler d1 migrations apply api-pphat-me-db --remote",
    "migrate:local": "npx wrangler d1 migrations apply api-pphat-me-db --local"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.7",
    "@types/node": "^25.6.0",
    "@types/nodemailer": "^8.0.0",
    "typescript": "^5.5.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.82.2"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "nodemailer": "^8.0.5"
  }
}
